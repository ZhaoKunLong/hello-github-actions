name: Workflow for the actions demo
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        
      - uses: ./actions-a
        with:
          MY_NAME: "the Name"
